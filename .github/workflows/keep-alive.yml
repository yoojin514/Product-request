name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 9 * * 1'  # 매주 월요일 오전 9시
  workflow_dispatch:       # 수동 실행도 가능

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s \
            -H "apikey: sb_publishable_Ln1Ghf2yN6YD8-NRtdXegQ_HEcf9AOR" \
            -H "Authorization: Bearer sb_publishable_Ln1Ghf2yN6YD8-NRtdXegQ_HEcf9AOR" \
            "https://vsqfnohvkhgpzdvfmqqv.supabase.co/rest/v1/settings?select=key&limit=1"
          echo "✅ Supabase 핑 완료"
